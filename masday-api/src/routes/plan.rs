//! Plan routes

use axum::{Json, Router};

pub fn plan_routes() -> Router {
    Router::new()
        // Placeholder for plan-specific routes
}

// Plan-specific routes are handled in workflow.rs for now
